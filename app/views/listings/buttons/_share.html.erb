<button class="<%= header_buttons_classes("indigo", 50) %>">
  <svg viewBox="0 0 24 24" fill="currentColor" class="w-4 h-4 mt-[4px] mr-2">
    <path fill-rule="evenodd" d="<%= share_svg_path_d %>" clip-rule="evenodd" />
  </svg>
  Share
</button>
